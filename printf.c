#include <stdarg.h>
#include "string.h"
#include "main.h"

/**
 * _printf - print
 * @format: first arg
 * @:
 * Return: int
 */

int _printf(const char *format, ...)
{
	va_list args;
	int i, count, d;

	count = 0;
	va_start(args, format);

	va_end(args);

	return (count);
}
